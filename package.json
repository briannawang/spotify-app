{
  "name": "spotify-app",
  "version": "0.1.0",
  "homepage": "https://briannawang.github.io/spotify-app",
  "private": true,
  "engines": {
    "node": "14.17.5"
  },
  "dependencies": {
    "color.js": "^1.2.0",
    "jquery": "^3.6.0",
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-scripts": "^5.0.1",
    "dotenv": "^16.0.1",
    "express": "^4.17.1",
    "http-proxy-middleware": "^2.0.6",
    "npm-run-all": "^4.1.5",
    "request": "^2.88.2"
  },
  "scripts": {
    "start": "react-scripts start",
    "heroku-postbuild": "SLUG=https://spotify-app-bw.herokuapp.com npm install && npm run build",
    "start-client": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "server": "node server",
    "dev": "SLUG=http://localhost:3000 run-p server start-client",
    "predeploy": "SLUG=https://spotify-app-bw.herokuapp.com npm run build"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },

  "proxy": "http://localhost:3001"
}
